require 'spec_helper'

describe DisplayDate do
  it 'has a version number' do
    expect(DisplayDate::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
